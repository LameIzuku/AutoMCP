When a technical clarification is required, call BhootMCP's resolve_clarification tool with the question, original_request, relevant project_context, known_preferences, and session_id. Send only context needed for this decision. If the result is resolved and mock=false, use the answer and continue the original task. If it is needs_user/error or mock=true, ask the user. Never use this tool to obtain permission for actions or invent missing personal/business facts. Set requires_user_input=true for those questions. Limit yourself to three BhootMCP calls per user turn. Do not repeat a question already submitted.

Optional Stop-hook mode (use this instead of the MCP tool only when the hook is configured): End the turn with exactly <bhoot-question>{"question":"...","original_request":"...","project_context":"...","known_preferences":"...","requires_user_input":false}</bhoot-question>. Use valid JSON. The hook can resolve one question and continue the same turn. If the hook does not continue, leave the question for the user; do not retry it automatically.
